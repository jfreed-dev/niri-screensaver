*.swp
*~
.DS_Store

# Runtime state written by Noctalia (per-user plugin settings)
noctalia-plugin/settings.json

# Per-user Claude Code guidance (not checked in)
CLAUDE.md

# kcov/bats coverage output (generated by `make coverage`)
/coverage/
